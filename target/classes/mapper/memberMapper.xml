<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<insert id="signUp" parameterType="MemberVO">
		insert into member
		(member_name, member_password, member_nickname, member_age,
		member_phone, member_job, member_local, member_email)
		values (
		#{member_name}, #{member_password}, #{member_nickname},
		#{member_age},
		#{member_phone},
		#{member_job},
		#{member_local},
		#{member_email}
		)
	</insert>
	<!-- 아이디체크 -->
	<select id="memberIdCheck" parameterType="String"
		resultType="String">
		select member_name
		from member
		where member_name = #{member_name}
	</select>

	<!-- 비밀번호체크 -->
	<select id="memberPwCheck" parameterType="String"
		resultType="String">
		select member_password
		form member
		where member_password =
		#{member_password}
	</select>

	<!-- 로그인 진행 -->
	<select id="memberLogin" parameterType="memberVO"
		resultType="String">
		select member_password from member where member_id
		=#{member_id}
	</select>

	<select id="getMemberByName" parameterType="String"
		resultType="memberVO">
		select * from member where member_name =#{member_name}
	</select>
<!-- 비번 체크용 -->
	<select id="getMemberByPassword" parameterType="String"
		resultType="memberVO">
		select * from member where member_password =#{member_password}
	</select>
<!-- 	아이디찾기용 -->
	<select id="getMemberByID" parameterType="int"
		resultType="memberVO">
		select * from member where member_id =#{member_id}
	</select>

<!-- 	아이디찾기용 -->
	<select id="getMemberByEmail" parameterType="String"
		resultType="memberVO">
		select * from member where member_email =#{member_email}
	</select>
	

	<update id="memberInfoUpdate" parameterType="memberVO">
		update member
		set
		member_password = #{member_password},
		member_nickname = #{member_nickname},
		member_age = #{member_age},
		member_phone = #{member_phone},
		member_job = #{member_job},
		member_local = #{member_local},
		member_email = #{member_email},
		member_money = #{member_money}
		where member_id = #{member_id}
	</update>

	<update id="memberPWUpdate" parameterType="memberVO">
		update member
		set
		member_password = #{member_password}
		where member_id = #{member_id}
	</update>

</mapper>










